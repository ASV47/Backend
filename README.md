# Codeikoo Todo API (.NET 8 Web API + SQLite)

A small CRUD API used by the Angular UI.

## Requirements
- .NET SDK 8.x

## Run
```bash
dotnet restore
dotnet run
```

Swagger will be available on:
- https://localhost:5001/swagger (or similar)

## CORS
CORS is enabled for:
- http://localhost:4200
